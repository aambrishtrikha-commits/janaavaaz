# Reliability matrix

Default for judges: APP_MODE=demo (fixtures + live Gemini if key present).
Video is always recorded against fixtures.

Publish is blocked unless brief contains citations[] or explicit insufficient_official_data=true.
Idempotent submit: client_request_id.
Timezone: Asia/Kolkata on all public stamps.
