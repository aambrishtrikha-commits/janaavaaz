# Build phases — freeze before adding theatre

P0 is the contest spine. P1 is Google-wow on that spine. P2 is engagement rooms.
Nothing in P2 ships until the P0 corridor works with fixtures.

## P0 — Corridor (must freeze)

1. Pack loader + cited indicators (no silent numbers).
2. Citizen submit: text (voice/photo upload accepted).
3. Gemini Hearing via structured output + function calls into the pack.
4. Gemini Brief + Dissent grounded only on pack cells.
5. Officer 12-minute board: Publish / Merge / Send back.
6. PNR tracker for the citizen.
7. Multi-state map (MH + AS packed; others awaiting pack).
8. Docker Compose + Cloud Run Dockerfile.
9. Fixture mode for video and eval week.

## P1 — Google platform depth

1. Live API Hearing with tool chips (fallback to P0 upload).
2. Maps grounding snapped to LGD.
3. Vertex embeddings + Gemini judge for merge.
4. BigQuery national ask (or SQL equivalent if BQ project absent).
5. Context cache / File Search over context/.
6. Glass-box drawer (model, region, tools, citations).

## P2 — Stay / explore

Echo wall, twin district, 12-script wall, season ring, night digest, webhook simulator.

## Law

File first. Theatre second. Gemini never invents crores.
